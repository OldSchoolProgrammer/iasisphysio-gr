# Copilot Instructions

## Current repository state

This repository is currently empty from the working tree perspective: no source files, README, CONTRIBUTING guide, build manifests, test configuration, or existing AI assistant instruction files were detected when this file was created.

## Build, test, and lint commands

No build, test, or lint commands are defined yet.

When project files are added later, inspect the root and any workspace/package directories for the primary manifest before assuming the stack:

- `package.json`
- `pyproject.toml`
- `Cargo.toml`
- `go.mod`
- `*.sln` / `*.csproj`
- `pom.xml`
- `build.gradle*`
- `Makefile` / `justfile`

Only document commands that actually exist in the repository, including the most specific available command for running a single test.

## High-level architecture

No application architecture is present yet.

If the repository gains code later, derive architecture from the actual entry points, package boundaries, runtime wiring, and data flow rather than from directory names alone.

## Key conventions

No repository-specific conventions were documented in project files at the time this file was created.

If conventions are added later in README files, contribution guides, test helpers, build scripts, or other assistant configuration files, fold those repository-specific rules into this document instead of adding generic advice.
